export const environment = {
  production: false,
  
  // URL para el entorno local (El backend de Spring Boot corre en el puerto 8080 por defecto)
  apiUrl: 'http://localhost:8080',

  // ---------------------------------------------------------
  // CREDENCIALES DE PRODUCCIÓN (Descomentar para producción)
  // ---------------------------------------------------------
  // production: true,
  // apiUrl: 'https://agrocyt-backend.onrender.com'
};